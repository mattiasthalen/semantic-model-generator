"""Utility modules for semantic model generation.

This package contains pure-function utilities for:
- UUID generation (deterministic IDs for stable regeneration)
- TMDL identifier quoting (handling special characters in names)
- TMDL whitespace validation (ensuring tab-only indentation)
"""
